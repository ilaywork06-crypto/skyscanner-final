# What never belongs in a build context. Every image installs its own dependencies and builds its own
# output, so shipping either of those across only makes the context slower to send and the layers wrong.
node_modules
**/node_modules
dist
**/dist
.venv
.mypy_cache
.git
*.log
