{
  "name": "truth-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/ag-grid-ts",
    "packages/core-ui",
    "frontend",
    "truth/frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace skyscanner-frontend",
    "dev:truth": "npm run dev --workspace truth-frontend",
    "build": "npm run build --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run type-check --workspaces --if-present"
  }
}
